import React from 'react';
import {
  View,
} from 'react-native';

export default function JobPostScreen({navigation}) {

  return (
    <View
      style={{
        flex:1,
        justifyContent: 'center',
        alignItems: 'center',
        backgroundColor:'red',
      }}>
    </View>
  );
}
