import React, {useState, useRef, useEffect} from 'react';
import {
  ScrollView,
  TouchableOpacity,
  View,
  BackHandler,
  Text,
} from 'react-native';

export default function HomeDashboard({navigation}) {
  return (
    <View style={{flex: 1}}>
      <Text>home</Text>
    </View>
  );
}
